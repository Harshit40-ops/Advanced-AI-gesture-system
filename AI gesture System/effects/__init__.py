"""Futuristic visual-effect overlays rendered on top of the camera frame.

Pure rendering: these modules take a frame + some geometry and draw onto it.
They know nothing about gestures or actions -- the main loop decides when to
invoke them.
"""
